<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.getmobileltd.trafficbar">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".application.TrafficBarApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".checkout.CheckoutActivity"></activity>
        <activity android:name=".dashboard.mycart.addtocart.AddToCartActivity" />
        <activity android:name=".dashboard.profile.editprofile.EditProfileActivity" />
        <activity android:name=".registration.login.mvp.LoginActivity" />
        <activity android:name=".registration.register.confirmregister.ConfirmRegisterActivity" />
        <activity
            android:name=".dashboard.DashboardActivity"
            android:hardwareAccelerated="true" />
        <activity android:name=".orderfood.menudetails.MenuDetailsActivity" />
        <activity android:name=".orderfood.menulist.ListAvailableFoodActivity" />

        <activity android:name=".registration.register.SignUpActivity">

        </activity>
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
        <activity android:name=".intro.IntroActivity" >


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>